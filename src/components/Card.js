import React from "react";

const Card = ({ name }) => {
    return (
        <div className="f8 link br2 ph3 pv2 mb2 bg-transparent menu-button">
            {name}
        </div>
    );
};

export default Card;
